namespace ElectronicsShop.Models
{
    public enum MenuType
    {
        Upper = 1,
        Lower = 2
    }

    public enum OrderStatus
    {
        Created = 0,
        Accepted = 1,
        Cancelled = 2,
        Completed = 3
    }
}